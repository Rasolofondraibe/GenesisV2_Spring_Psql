public ResponseEntity<Page<[returnType]>> crudpage(@RequestParam(defaultValue = "0") int page,@RequestParam(defaultValue = "10") int size){
    return ResponseEntity.ok((Page<[returnType]>)repo.findAll(PageRequest.of(page, size)));
}